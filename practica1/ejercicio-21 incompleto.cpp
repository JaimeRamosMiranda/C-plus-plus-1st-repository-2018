/*
21.- Se tienen cuatro esferas A, B, C y D de las cuales se sabe que tres son de igual peso y una diferente. 
Realice un programa en C que reciba como datos los pesos de las cuatro esferas y 
determine cual es la esfera diferente y si es de mayor o menor peso.
*/

#include <iostream>
#include <conio.h>
using namespace std;

int main()
{
	//declaracion
	int	a,b
	//entrada
	cout<<"Ingresar el peso de la esfera A :"<<endl;
	cin>>a;
	cout<<"Ingresar el peso de la esfera B :"<<endl;
	cin>>b;
	//proceso
	if (a==b)
		cout<<"Las esferas A y B tienen el mismo peso"<<endl;
		cout<<"Ingresar el peso de la esfera C"<<endl;
		if (c=a)
		c=a
		cin>>c;
		
		if (c==a)
			cout<<"Las esferas A,B y C tienen el mismo peso"<<endl;
			cout<<"Ingresar el peso de la esfera D"<<endl;
			
	else	
		cout<<"Las esferas A y B tienen diferente peso"<<endl;
		cout<<"Ingresar el peso de la esfera C"<<endl;
		cin>>c;	
	//salida
	
	getche();
	return 0;
}
